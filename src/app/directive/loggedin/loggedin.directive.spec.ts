import { LoggedinDirective } from './loggedin.directive';

describe('LoggedinDirective', () => {
  it('should create an instance', () => {
    const directive = new LoggedinDirective();
    expect(directive).toBeTruthy();
  });
});
